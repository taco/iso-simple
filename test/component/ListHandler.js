import React from 'react/addons'
import assert from 'assert'
import ListHandler from '../../src/shared/components/ListHandler'

describe('Meal list handler compenent', () => {

	it('something 23', () => {
		assert(true)
	})

	it('something 24', () => {
		assert(true)
	})
})